# Streamlit Cloud secrets template — generated by vault-seed apply agent.
# Paste this into Streamlit Cloud > App settings > Secrets after replacing
# any <SET_ME> placeholders. Never commit the rendered file.

SUPABASE_URL = "{SUPABASE_URL}"
SUPABASE_SERVICE_ROLE_KEY = "{SUPABASE_SERVICE_ROLE_KEY}"
SUPABASE_ANON_KEY = "{SUPABASE_ANON_KEY}"
